{
  "dxt_version": "0.1",
  "name": "business-central-mcp",
  "display_name": "Business Central",
  "version": "1.3.0",
  "description": "Direct access to Microsoft Dynamics 365 Business Central via its native WebSocket protocol.",
  "author": {
    "name": "Torben Leth",
    "email": "sshadows@sshadows.dk"
  },
  "homepage": "https://github.com/SShadowS/business-central-mcp",
  "license": "MIT",
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "dist/stdio-server.js",
    "mcp_config": {
      "command": "npx",
      "args": [
        "-y",
        "business-central-mcp"
      ],
      "env": {
        "BC_BASE_URL": "${user_config.bc_base_url}",
        "BC_USERNAME": "${user_config.bc_username}",
        "BC_PASSWORD": "${user_config.bc_password}",
        "BC_PROFILE": "${user_config.bc_profile}"
      }
    }
  },
  "user_config": {
    "bc_base_url": {
      "type": "string",
      "title": "BC base URL",
      "description": "Business Central server base URL, e.g. http://your-bc-server/BC",
      "required": true
    },
    "bc_username": {
      "type": "string",
      "title": "BC username",
      "description": "NavUserPassword username for the BC server.",
      "required": true
    },
    "bc_password": {
      "type": "string",
      "title": "BC password",
      "description": "NavUserPassword password for the BC server.",
      "sensitive": true,
      "required": true
    },
    "bc_profile": {
      "type": "string",
      "title": "BC profile",
      "description": "Optional profile id, e.g. BUSINESS MANAGER. Affects which Role Center loads and which pages Tell Me indexes.",
      "required": false
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20"
    }
  }
}
