{
  "dxt_version": "0.1",
  "name": "business-central-mcp",
  "display_name": "Business Central",
  "version": "1.5.0",
  "description": "Direct access to Microsoft Dynamics 365 Business Central via its native WebSocket protocol.",
  "author": {
    "name": "Torben Leth",
    "email": "sshadows@sshadows.dk"
  },
  "homepage": "https://github.com/SShadowS/business-central-mcp",
  "license": "MIT",
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "dist/stdio-server.js",
    "mcp_config": {
      "command": "npx",
      "args": [
        "-y",
        "business-central-mcp"
      ],
      "env": {
        "BC_BASE_URL": "${user_config.bc_base_url}",
        "BC_USERNAME": "${user_config.bc_username}",
        "BC_PASSWORD": "${user_config.bc_password}",
        "BC_CLIENT_ID": "${user_config.bc_client_id}",
        "BC_PROFILE": "${user_config.bc_profile}"
      }
    }
  },
  "user_config": {
    "bc_base_url": {
      "type": "string",
      "title": "BC base URL",
      "description": "On-prem URL (http://server/BC) or SaaS portal URL (https://businesscentral.dynamics.com/{tenant}/{environment}).",
      "required": true
    },
    "bc_username": {
      "type": "string",
      "title": "BC username",
      "description": "NavUserPassword username. Leave empty for SaaS / OAuth.",
      "required": false
    },
    "bc_password": {
      "type": "string",
      "title": "BC password",
      "description": "NavUserPassword password. Leave empty for SaaS / OAuth.",
      "sensitive": true,
      "required": false
    },
    "bc_client_id": {
      "type": "string",
      "title": "Entra client ID",
      "description": "For bc_query on BC Online: multi-tenant public Entra app with delegated Dynamics 365 Business Central user_impersonation. UI tools do not need it.",
      "required": false
    },
    "bc_profile": {
      "type": "string",
      "title": "BC profile",
      "description": "Optional profile id, e.g. BUSINESS MANAGER. Affects which Role Center loads and which pages Tell Me indexes.",
      "required": false
    }
  },
  "compatibility": {
    "platforms": [
      "darwin",
      "win32",
      "linux"
    ],
    "runtimes": {
      "node": ">=20"
    }
  }
}
